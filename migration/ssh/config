# SSH private keys are intentionally not stored in this repository.
# Copy the private key through an encrypted, private channel, or create a new
# key on the destination system and register only its public key with GitHub.
Host arching3.github.com
    HostName github.com
    User git
    IdentityFile ~/.ssh/id_rsa
    IdentitiesOnly yes
